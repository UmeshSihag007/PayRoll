@using ApwPayrollWebApp.Models
@model MasterModel
@{
    ViewData["Title"] = "Designation";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <div class="card">
        <div class="card-header">
            <span class="card-title fw-normal">Add Designation</span>
        </div>    
        <div class="card-body">
            <form asp-action="CreateDesignation" method="post">
                <input asp-for="createDesignation.Id" hidden />
                <div class="row g-3">
                    <div class="col-md-4">
                        <label asp-for="createDesignation.Name" class="form-label">Name</label>
                        <input asp-for="createDesignation.Name" type="text" class="form-control" placeholder="Enter designation name" />
                        <span asp-validation-for="createDesignation.Name" class="text-danger"></span>
                    </div>
                    <div class="col-md-4">
                        <label asp-for="createDesignation.Description" class="form-label">Description</label>
                        <input asp-for="createDesignation.Description" type="text" class="form-control" placeholder="Enter description" />
                        <span asp-validation-for="createDesignation.Description" class="text-danger"></span>
                    </div>
                    <div class="col-md-12">
                        <div class="d-flex justify-content-end">
                        <button type="submit" class="btn btn-success ">
                            @(Model.createDesignation != null && Model.createDesignation.Id.HasValue && Model.createDesignation.Id.Value != 0 ? "Update" : "Add")
                        </button>
                    </div>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <div class="card mt-4">
        <div class="card-header">
            <span class="card-title fw-normal">Designation List</span>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead class="table-success">
                        <tr>
                            <th class="rounded-start">Sr.No</th>
                            <th>Name</th>
                            <th>Description</th>
                            <th class="rounded-end">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (ViewBag.Designation != null)
                        {
                            @foreach (var item in ViewBag.Designation)
                            {
                                <tr>
                                    <td>@(ViewBag.Designation.IndexOf(item) + 1)</td>
                                    <td>@item.Name</td>
                                    <td>@item.Description</td>
                                    <td>
                                        <a asp-action="DesignationView" asp-route-id="@item.Id" class="btn btn-sm btn-info editBtn">
                                            <i class="bi bi-pencil-square"></i>
                                        </a>
                                        <a class="btn btn-sm btn-danger" onclick="confirmDelete('@item.Id', 'refernece', '@Url.Action("Delete", "Designation")')">
                                            <i class="bi bi-trash-fill"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section scripts {
    <!-- dropzone min -->
    <script src="~/assets/libs/dropzone/dropzone-min.js"></script>

    <!--crypto-kyc init-->
    <script src="~/assets/js/pages/crypto-kyc.init.js"></script>

    <!-- App js -->
    <script src="~/assets/js/app.js"></script>

    <partial name="_ValidationScriptsPartial" />
}

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
@Html.Partial("_DeleteConfirmation")

<script>
    function confirmDelete(id) {
        Swal.fire({
            title: 'Are you sure?',
            text: "You won't be able to revert this!",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Yes, delete it!'
        }).then((result) => {
            if (result.isConfirmed) {
                let url = '@Url.Action("Delete", "Designation")/' + id;
                window.location.href = url;
            }
        });
    }
</script>
